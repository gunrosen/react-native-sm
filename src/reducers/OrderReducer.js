import {
    ORDER_INIT,

}from '../actions/types';
const STATE_INIT = {

}

export default (state = STATE_INIT, action) => {
    switch (action.type) {
      case STATE_INIT:
     
      default:
        return state;
    }
};
  